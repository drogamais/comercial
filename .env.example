# ── Base de Dados da Aplicação ──────────────────────────────────────────────
DB_ROOT_PASSWORD=rootpass
DB_NAME=comercial_dev
DB_USER=app
DB_PASSWORD=apppass

# ── Base de Dados Externa (read-only: GTINs, nomes ajustados) ───────────────
EXTERNAL_DB_HOST=10.48.12.20
EXTERNAL_DB_PORT=3306
EXTERNAL_DB_NAME=drogamais
EXTERNAL_DB_USER=
EXTERNAL_DB_PASSWORD=

# ── Power Embedded API ───────────────────────────────────────────────────────
EMBEDDED_API_KEY=

# ── Autenticação (Authentik OIDC) ────────────────────────────────────────────
# Definir como "false" para bypass local (mock user, sem Authentik)
REQUIRE_AUTH=false
AUTHENTIK_ISSUER=https://auth.empresa.com/application/o/comercial/
AUTHENTIK_CLIENT_ID=
AUTHENTIK_CLIENT_SECRET=

# ── Armazenamento de Ficheiros ───────────────────────────────────────────────
# Caminho para guardar contratos PDF (dentro do container ou volume montado)
UPLOAD_FOLDER=/mnt/contratos

# ── Segurança ────────────────────────────────────────────────────────────────
DELETE_PASSWORD=com123

# ── Frontend ─────────────────────────────────────────────────────────────────
VITE_API_BASE_URL=http://localhost:3001
FRONTEND_ORIGIN=http://localhost:5173
